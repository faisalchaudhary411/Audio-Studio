runpod==1.7.7
torch==2.4.0
torchaudio==2.4.0
numpy
# chatterbox-tts itself is intentionally NOT pinned here — it's installed
# from source in the Dockerfile instead, with the broken pkuseg dependency
# stripped out first. See the Dockerfile comment for why.
