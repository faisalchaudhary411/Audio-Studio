Flask==3.0.3
gunicorn==22.0.0
edge-tts>=7.2.7
gTTS==2.5.4
lameenc==1.8.4
werkzeug==3.0.3
pydub==0.25.1
audioop-lts==0.2.1; python_version >= "3.13"
SpeechRecognition==3.10.4
markdown==3.7
requests==2.32.3
numpy==1.26.4
noisereduce==3.0.3
Pillow>=10.0.0
pytesseract>=0.3.10
librosa==0.10.2.post1
soundfile==0.12.1
psutil==6.0.0
sentry-sdk[flask]>=2.0.0
pyloudnorm==0.1.1
# Studio-quality denoise (DeepFilterNet AI speech enhancement).
# TWO one-time VPS setup steps are REQUIRED before `pip install -r
# requirements.txt` will succeed here -- do both, in order, every time
# this is set up on a fresh VPS/venv:
#
# 1. Install Rust (deepfilternet's DeepFilterLib component has no
#    pre-built wheel for most Linux/Python combos -- pip has to compile
#    it from source, which needs cargo on PATH):
#      curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
#      source "$HOME/.cargo/env"
#
# 2. Install CPU-only torch + torchaudio BEFORE running this file.
#    This VPS has no GPU, but plain `pip install torch` / `torchaudio`
#    from PyPI pulls the full CUDA builds (cublas, cudnn, triton, nccl...
#    several GB of dead weight). Installing the CPU builds first satisfies
#    the pins below without ever touching the CUDA packages:
#      pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Skip step 1 and deepfilternet's build fails with "Cargo ... is not
# installed". Skip step 2 and a plain `pip install -r requirements.txt`
# silently drags in ~5GB of unused CUDA packages. Missing torchaudio
# causes: ModuleNotFoundError: No module named 'torchaudio' when Studio
# denoise runs (df/io.py imports it).
torch>=2.0.0
torchaudio>=2.0.0
deepfilternet>=0.5.6
